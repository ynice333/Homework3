# Нужно вернуть исправленную версию, которая начинается с заглавной буквы и заканчивается точкой.

def correct_sentence(text):
    text = str(text)
    return print.title(str(text))


print(correct_sentence(text = 'greetings, friends'))